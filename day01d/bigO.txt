The big O notation for binary search is O(log n), because the list is cut half.

If array was bigger, with over 100 elements, the first iteration would be cut half and the list would be 50 elements from now. At the 3 iteration would be 25 elements. 